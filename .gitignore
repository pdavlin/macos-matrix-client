# Local-only project management coordinates
.plane.local.md

# Claude Code personal settings
.claude/settings.local.json

# Upstream (Mactrix)
xcuserdata/
.DS_Store
build/

# Claude Code runtime files
.claude/scheduled_tasks.lock

# Swift Package Manager build products (spike/)
.build/

# Timeline spike measurement dumps. The "Dump stats" button writes to the
# process working directory, which is usually the repo root; those are scratch.
timeline-spike-*.json
# Dumps under spike/results are the recorded baseline the gate scores against
# (S-39), so they are tracked.
!spike/results/timeline-spike-*.json
