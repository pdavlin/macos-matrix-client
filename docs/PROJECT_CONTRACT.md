# Project Contract — Native macOS Matrix Client

| | |
|---|---|
| **Working name** | TBD (repo placeholder: `matrix-mac-client`) |
| **Base codebase** | Fork of [Mactrix](https://github.com/viktorstrate/mactrix) (SwiftUI + matrix-rust-sdk) |
| **Owner** | Patrick |
| **Status** | Draft v0.1 — 2026-08-19 |
| **Purpose of this doc** | Source of truth for scope and sequencing. PRDs are derived from milestones; epics from feature clusters; stories/tasks from epics. Changes to scope require an amendment logged in §11. |

---

## 1. Vision

A genuinely native, "Mac-assed" Matrix client for a single power user running a self-hosted homeserver with mautrix bridges. All messaging — bridged WhatsApp, Signal, Discord, etc. and native Matrix — in one fast, low-footprint AppKit-feeling app with a compliant Liquid Glass navigation layer, real menus, real keyboard support, and best-in-class local search. Not a general-audience product, not cross-platform, not a Beeper competitor. The measure of success: Beeper and Element Desktop can be uninstalled.

## 2. Guiding principles

1. **Don't implement Matrix.** matrix-rust-sdk owns sync, E2EE, crypto store, verification, timeline state. The app is a SwiftUI/AppKit shell over the FFI bindings. Any temptation to hand-roll protocol logic is a design smell.
2. **Glass on the navigation layer only.** Per Apple HIG: toolbar, sidebar, sheets, popovers get Liquid Glass (mostly automatic when built with the current SDK). The message timeline is opaque content. Never `.glassEffect()` on scrollable content.
3. **Mac-assed over feature-complete.** A smaller feature set that fully supports menus, shortcuts, drag-and-drop, context menus, multiple windows, and state restoration beats a bigger one that doesn't. Platform conventions are requirements, not polish.
4. **Personal scale.** Optimize for one account, one user, hundreds of (mostly bridged DM) rooms. No design work for org-scale spaces, moderation tooling, or thousands of public rooms.
5. **Hard fork.** Mactrix is a starting point, not an upstream. No obligation to stay mergeable. Its chrome is replaced wholesale; its SDK wiring, session storage, and verification flow are inherited and refactored in place.
6. **Scope discipline is the survival mechanism.** Single maintainer + agentic coding. Deferred means deferred; §11 amendments are the only path back in.

## 3. Scope

### In scope (v1 = M0–M3)
Login/SSO against own homeserver, device verification, E2EE, room list, full read/write timeline (text, replies, edits, reactions, redactions, media), local notifications, Mac platform integration (menus, shortcuts, windows, drag-and-drop, Quick Look), local full-text message search.

### Deferred (post-v1, requires amendment to schedule)
- Voice/video calls (Element Call / VoIP) — largest single scope item; explicitly held back
- Threads (beyond rendering a "thread exists" affordance)
- Spaces (bridge-network grouping may substitute; see Q-6)
- Multi-account
- Widgets, location sharing, polls (render-only fallback acceptable)
- Custom sticker/emoji *management* (rendering incoming ones is M4)
- Public distribution: notarization, auto-update (Sparkle), website
- iOS/iPadOS ports of any kind

### Out of scope (never, absent a new contract)
Account registration flows, homeserver administration UI, bridge management UI (bridges are managed server-side via their own tooling), Windows/Linux, non-Matrix protocols in-app.

## 4. Feature roadmap

### M0 — Foundation: fork, build, inherit
- Fork Mactrix; building and running in current Xcode on Apple Silicon
- Pin `matrix-rust-components-swift` to a known-good version; confirm the shipped XCFramework includes a macOS slice, else stand up the from-source build (Rust toolchain, `aarch64-apple-darwin`) — see R-3
- Inherited-and-working: login, session persistence (keychain), device verification (emoji), room decryption
- Repo hygiene for agentic dev: `CLAUDE.md`, exported Xcode SwiftUI agent skills (`xcrun agent skills export`), vendored copies of the generated FFI Swift interfaces so the agent can read real signatures, SwiftFormat + SwiftLint, `xcodebuild` build/test scripts
- **Timeline spike (gate for M1):** throwaway prototype of the inverted, variable-height, back-paginating timeline at 10k+ events with edits/reactions mutating laid-out items. Decide pure-SwiftUI vs AppKit-backed (`NSTableView`/`NSCollectionView` via `NSViewRepresentable`) before building the real one.
- Exit criteria: clean build from `git clone` + documented steps; logs into own homeserver; decrypts an E2EE room; timeline architecture decision recorded in §11.

### M1 — Core messaging (daily-driver read path)
- Replace Mactrix chrome: `NavigationSplitView`, glass toolbar/sidebar via standard components, opaque content layer
- Room list: recency sort, unread badges, DM/room distinction, bridge-aware avatars and display names, mute state
- Timeline v1 (per M0 decision): render text/media/state events, day separators, sender grouping, back-pagination, read markers, typing indicators
- Compose v1: plain text send, replies
- Media: inline image/video thumbnails, Quick Look on click, save-to-Downloads
- Notifications: local (in-process sync loop → `UNUserNotificationCenter`), dock badge, per-room mute respected. No APNs, no push gateway — the app is expected to be running.
- Exit criteria: a full day of bridged DM traffic handled without opening Beeper/Element; no timeline scroll hitches at p95.

### M2 — Full send path + Mac platform integration
- Edits, reactions (picker + frequent-emoji row), redactions, message forwarding
- Media send: file picker, paste, drag-and-drop into composer; upload progress
- Markdown composer with mentions autocomplete; multiline behavior (Enter/Shift-Enter) configurable
- Menu bar: real File/Edit/View/Go menus; Cmd-K quick-open room switcher; standard shortcuts (Cmd-F reserved for M3, Cmd-1..9 room pinning, Cmd-Shift-] room cycling)
- Context menus on every message and room; text selection across messages
- Multiple windows (room-in-new-window) + state restoration; sidebar width and window frames persisted
- Settings window (appearance, notifications, composer behavior) as a proper macOS Settings scene
- Exit criteria: everything currently done daily in Beeper — minus calls — has a keyboard-reachable equivalent.

### M3 — Search & archive (the differentiator)
Rationale: Element X still ships without in-room search; for a bridges-as-archive setup, search is the killer feature.
- Local message index: SQLite FTS5 populated from the SDK's event cache/timeline stream; index survives restarts; backfill job for history
- In-room search (Cmd-F) with jump-to-context in timeline
- Global search (Cmd-Shift-F) across all rooms, filterable by sender/room/date/has-media
- Per-room media browser grid
- Exit criteria: any remembered message from any bridged network found in under a second; index size and privacy posture documented (index is plaintext-at-rest — see R-6).

### M4 — Refinement (grab-bag, individually optional)
- Custom emoji/sticker rendering (MSC2545-style packs incoming)
- Read-only thread view
- Share extension (share into a room from Finder/Safari)
- App Intents / Shortcuts (send message, set status)
- Icon Composer app icon; LG2 design-token pass
- Room creation/invite management (minimal)
- Multi-account (test vs. prod accounts) if separate build configs prove annoying

## 5. Implementation order

Strictly sequential by milestone; within a milestone, order is decided at PRD time. Rationale for the sequence:

1. **M0 before anything** — the timeline spike is the single largest technical risk and must be retired before UI investment; FFI build viability gates the whole project.
2. **M1 read-path before M2 send-path** — value arrives earliest via reading; the send path builds on timeline primitives.
3. **M2 before M3** — search UX (jump-to-result) depends on a mature timeline that can open at an arbitrary event.
4. **M3 before M4** — the differentiator ships before comforts.

Cross-cutting, continuous (not milestone-gated): crash-free operation, memory/energy budget adherence (§7), decision log upkeep, SDK version pinning and a monthly upgrade window (R-2).

Suggested cadence for decomposition: **one milestone = one PRD**; epics = the bullet clusters above; stories sized to a single Claude Code session (≤ half a day) with build + tests green as the definition of done.

## 6. Environment & installation requirements

### Development machine
- Apple Silicon Mac; macOS 26 (Tahoe) minimum, macOS 27 beta if adopting LG2 tokens immediately (Q-3)
- Xcode 27 (beta channel until GM) — required for current Liquid Glass appearance and agent skills
- Free Apple ID sufficient: local/ad-hoc signing, no APNs, no Developer Program membership until public distribution (deferred)
- Keychain access entitlement for session storage (inherited from Mactrix's setup — verify in M0)

### Toolchain
- Swift Package Manager only (no CocoaPods/Carthage)
- `matrix-rust-components-swift` pinned by exact version in `Package.resolved`
- Rust toolchain via `rustup` **only if** R-3 forces from-source XCFramework builds: `aarch64-apple-darwin` target, `uniffi-bindgen` per matrix-rust-sdk's pinned version
- SwiftFormat, SwiftLint, xcbeautify
- swift-snapshot-testing for timeline rendering regression tests

### Agentic-dev setup (Claude Code)
- `CLAUDE.md` at repo root: build/test commands, architecture map, "never hand-roll protocol logic" rule, pointer to vendored FFI interfaces
- `xcrun agent skills export` output committed under `docs/skills/` (SwiftUI Specialist + What's New in SwiftUI) — Liquid Glass and 27-era APIs are undertrained; this is the hallucination guard
- Vendored generated FFI Swift sources under `docs/ffi/` (read-only reference copies), refreshed on every SDK bump
- Build validation loop the agent can run headlessly: `xcodebuild build test` scripts; snapshot tests as the agent's eyes on the timeline

### Server (homelab) — prerequisites, not project deliverables
- Synapse, recent stable (Simplified Sliding Sync / MSC4186 is required by matrix-rust-sdk and is native in modern Synapse — no sliding-sync proxy)
- PostgreSQL backing Synapse
- Federation optional but recommended; `.well-known` delegation for the chosen server name; reverse proxy with valid TLS
- mautrix bridges deployed as appservices (compose-managed), with end-to-bridge encryption and double-puppeting as desired
- Two accounts recommended: `@patrick:…` (prod) and `@dev:…` (test) so agent-driven runs never touch real DMs
- No TURN/coturn (calls deferred), no Sygnal/push gateway (no APNs)

## 7. Non-functional requirements

| Dimension | Budget / requirement |
|---|---|
| Idle memory | < 300 MB with full account synced (vs. ~1 GB+ Electron baseline) |
| Cold start to interactive room list | < 2 s on Apple Silicon |
| Timeline scroll | no visible hitching at p95; snapshot-tested layouts |
| Energy | negligible CPU at idle; sync loop must not prevent Mac sleep policy from applying |
| Offline | room list + cached timelines readable offline; queued sends on reconnect |
| Data at rest | crypto store per SDK defaults; search index encryption posture documented (R-6) |
| Accessibility | full keyboard operability (falls out of Mac-assed requirement); VoiceOver labels on timeline items; respect Reduce Transparency (glass degrades gracefully) |
| macOS target | min macOS 26; primary dev/test on latest |

## 8. Risks & mitigations

| ID | Risk | Mitigation |
|---|---|---|
| R-1 | SwiftUI timeline can't hit scroll/anchoring targets (the classic hobby-client killer) | M0 spike gates the decision; AppKit-backed fallback (`NSTableView`/`NSCollectionView` in `NSViewRepresentable`) is a planned branch, not an emergency |
| R-2 | matrix-rust-sdk / FFI API churn; thin docs; undertrained in models | Pin exact versions; monthly upgrade window with dedicated story; vendored FFI sources keep the agent honest; changelog review is part of every bump |
| R-3 | macOS is second-class in `matrix-rust-components-swift`; XCFramework may lack a usable macOS slice at some version | Verify in M0 before any UI work; maintain a documented from-source build path as insurance |
| R-4 | Liquid Glass churn (LG2 token updates, contrast/accessibility revisions) | Glass restricted to standard components on the nav layer → appearance updates arrive via SDK rebuild, not code |
| R-5 | Licensing: Mactrix's license and the dual-licensing of Element-published components could constrain a future public release | Q-1/Q-2 resolved during M0, before significant divergence; private personal use is safe in the interim |
| R-6 | M3 search index is plaintext-at-rest, weakening E2EE guarantees on disk | Document explicitly; FileVault assumed; evaluate SQLCipher or index-level encryption as an M3 story; index exclusion list for sensitive rooms |
| R-7 | Single-maintainer burnout / scope creep | §11 amendment process; milestone exit criteria are binary; M4 items individually droppable |
| R-8 | Agentic dev can't see the UI; visual regressions slip through | Snapshot tests as CI gate; human review reserved specifically for scroll feel and glass rendering |

## 9. Open questions (resolve by end of M0 unless noted)

- **Q-1:** Mactrix license — confirm terms permit fork + modification; record obligations.
- **Q-2:** `matrix-rust-components-swift` licensing/dual-licensing status at the pinned version — implications only if the app is ever distributed (deferred, but check before divergence makes unwinding costly).
- **Q-3:** Minimum deployment target: macOS 26 (stable, LG1) vs. macOS 27 (beta now, LG2, GM ~Sept 2026). Leaning 26-min/27-primary.
- **Q-4:** XCFramework macOS slice present at pinned version? (Feeds R-3.)
- **Q-5:** Keep Mactrix's session/crypto-store layout or migrate to current SDK defaults during M0 refactor?
- **Q-6:** Bridge-network grouping in the sidebar (WhatsApp/Signal/Discord sections) as a v1 substitute for Spaces — cheap win or premature structure? Decide at M1 PRD time.
- **Q-7:** Project name. (Blocking nothing; blocking everything.)
- **Q-8:** Target homeserver vs. the existing Beeper self-host hybrid on `sectional-cache.exe.xyz` (local `bbctl` bridges for Slack/Google Chat/GroupMe against Beeper's hosted homeserver, account `@pdav:beeper.com`). The contract assumes self-hosted Synapse + mautrix. Options: Beeper setup stays interim while homelab Synapse is stood up; the client targets `beeper.com` (breaks the dev-account rule, unverified MSC4186 support); or the Beeper bridges become a migration source. **Resolve before S-05.**

## 10. Working agreements

- This contract is the only source of scope truth. PRDs cite milestone IDs; epics cite roadmap bullets; stories cite epics.
- Definition of done for any story: builds clean, tests (incl. snapshots) pass, manual smoke on the dev account, no new SwiftLint violations.
- The prod account is never used for automated/agent-driven testing.
- Every architectural decision (timeline approach, index schema, store migrations) gets a dated entry in §11.
- Deferred items enter scope only via a logged amendment with an explicit trade (what moves out or which milestone absorbs the cost).

## 11. Decision log & amendments

| Date | Decision |
|---|---|
| 2026-08-19 | Contract drafted. Fork Mactrix as base; hard-fork stance; calls and threads deferred; search designated the v1 differentiator (M3). |
| 2026-08-19 | Q-8 opened: existing Beeper self-host bridges on sectional-cache.exe.xyz vs. the assumed homelab Synapse target. Undecided; resolve before S-05. |
| 2026-08-19 | Q-1 resolved: Mactrix is GPL-3.0-only. Fork and private modification are unrestricted; public distribution would require conveying the fork under GPL-3.0, providing complete source, and avoiding distribution channels (e.g. the Mac App Store) whose terms conflict with GPLv3. See THIRD_PARTY_NOTICES.md. |
| 2026-08-19 | Q-2 resolved: matrix-rust-components-swift and matrix-rust-sdk are both Apache-2.0 with no AGPL history; the 2023-2024 Element AGPL dual-licensing applies to Synapse and Element X client apps, not to matrix-rust-sdk. Apache-2.0 is compatible with GPLv3 combination. Mactrix pins matrix-rust-components-swift to `branch = main` rather than a fixed version; establishing an exact pin remains an open task, separate from this licensing verdict. See THIRD_PARTY_NOTICES.md. |
| 2026-08-19 | Q-4 resolved: matrix-rust-components-swift pinned to exact version 26.04.01 (revision 2916f3f9cc2aea86ba3a820cb6a8389e13e0284a, the same commit previously tracked via `branch = main`, so no functional change). The macOS slice is present in the resolved XCFramework (`LibraryIdentifier` `macos-arm64_x86_64`; the static lib carries both `arm64` and `x86_64` per `lipo -info`) and the app links and builds clean against it. The from-source build path for `aarch64-apple-darwin`, as insurance against a future pinned version dropping the macOS slice, is documented in `scripts/build-ffi-notes.md`. |
| 2026-08-19 | Q-3 resolved: minimum deployment target is macOS 26.0; primary dev and test run on macOS 27 beta with Xcode 27. Rationale: the current-SDK Liquid Glass appearance the contract requires needs the current SDK, and the pinned matrix-rust-sdk binary itself already targets macOS 26.2. |
| 2026-08-20 | S-15 timeline decision: the timeline is **AppKit-backed** — `NSTableView` wrapped in `NSViewRepresentable`, pre-measured row heights, measured-delta prepend compensation (per the S-14 spike candidate). Measured on the S-12 harness, 10k events, identical workload fingerprint, 120 Hz: prepend ×20 anchoring AppKit 20/20 samples at 0.0pt worst vs SwiftUI 18/20 at 0.3pt; mutation-storm-while-scrolling worst drift AppKit 815pt vs SwiftUI 4238pt; frame p95 at or under 8.5ms for both outside storm conditions. SwiftUI's below-viewport anchor gap is structural (`defaultScrollAnchor` covers only one edge); the losing candidate is deleted, the harness and both candidates' dumps are kept under `spike/results/` for M1 regression use. R-1 retired. |
| 2026-08-20 | Q-8 hosting resolved: Synapse runs on a dedicated exe.dev VM (primary) with davbuntu as cold-standby failover and permanent-migration target; hot failover ruled out (single-writer homeserver, E2EE one-time-key hazard). Migration plan in `docs/SYNAPSE-MIGRATION-PLAN.md`, copies on both hosts. Beeper hybrid on sectional-cache stays interim. STILL OPEN before account creation: the server name (davlin.io + .well-known delegation) — the one permanent choice. |
| 2026-08-20 | Q-8 fully closed (S-17): the Matrix server name is the apex, **davlin.io** — user IDs `@patrick:davlin.io` (prod) and `@dev:davlin.io` (test). Permanent and unchangeable. Delegation design: `/.well-known/matrix/server` and `/client` served by a Cloudflare Worker at the edge (site deploy pipeline out of the Matrix trust path; client file carries CORS `*`); delegation target is the Synapse VM's own `*.exe.xyz` hostname on 443, reached directly — the Cloudflare proxy stays out of the Matrix data path. Trust anchors: the Porkbun and Cloudflare accounts (2FA audit in S-19). Rationale and risk analysis on stories S-16/S-17. |
| 2026-08-24 | Q-5 resolved (S-06): the session/crypto store layout inherited from Mactrix is **kept** — it already matches current SDK defaults (`SqliteStoreBuilder`, one directory per random store ID holding `matrix-sdk-state.sqlite3` + `matrix-sdk-crypto.sqlite3`, passphrase-encrypted, data under Application Support and cache under Caches inside the sandbox container). Two fixes made while stores were cheap to reset: (1) the store parent directory is named by the app's own bundle ID (`io.davlin.matrixclient`) instead of upstream's `dk.qpqp.mactrix`; the single live store on the dev Mac was moved by hand, no code migration shipped. (2) Orphaned store directories (each abandoned login attempt leaks one) are swept on successful session restore. Keychain decisions: the session stays one JSON blob in the legacy login keychain (`kSecUseDataProtectionKeychain: false`) because local builds have no development team and data-protection mode fails with `-34018` without a real application identifier; revisit only if a team-signed distribution appears. The fixed `UserSession` keychain key has no per-account namespacing — acceptable under the single-account contract (§4), and `retrieveSessionFromKeychain` already rejects a user-ID mismatch. Restore verified end to end: quit/relaunch reused device `MHZQPGAEHX` with no new device minted on the homeserver. |
| 2026-08-24 | S-07 E2EE trust validated on the dev account: emoji SAS verification between Mactrix and Element Web (both directions exercised), cross-signing trust confirmed server-side via `/keys/query` signatures, key backup v1 created, and a fresh verified Mactrix session decrypts pre-existing encrypted history. Incident during the story: the account's original cross-signing private keys were unrecoverable — they were bootstrapped by an Aug 20 integration-test session whose store the S-06 orphan sweep later deleted, and no secret storage existed yet — so the cryptographic identity was reset from Element Web. The recovery key now exists and is held in `.claude/settings.local.json` (`MATRIX_DEV_RECOVERY_KEY`). Gaps found, filed as stories: S-24 (the app does not surface an own-identity reset; it fails SAS at the MAC stage with no explanation) and S-25 (no recovery-key entry UI; backup restore is impossible without a second live device — the SDK API exists unused in the FFI). Ops note: repeated login cycles during E2EE testing exhaust Synapse's default `rc_login` limits; raise them in a conf.d override if this recurs. |
| 2026-08-27 | S-08 E2EE decryption validated on the dev account, and a store-path defect from S-06 found and fixed. Validation (local encrypted test room; bridged-DM validation stays deferred to bridge deployment per the approved amendment): ten live messages from Element Web all decrypted on arrival, and history rendered readable across two consecutive quit/relaunch cycles, with zero UTDs reported in either case. Decisions: (1) the app now records UTDs. `LiveTimeline` sets `TimelineConfiguration.reportUtds: true` (it was `false`, so every decryption failure was silent) and `MatrixClient.startSync` installs a `UtdReporter` via `Client.setUtdDelegate`, which logs each UTD's `UtdCause`, late-decrypt time, event age against device age, own-identity trust, and both homeservers to the `utd` log category. `UtdCause` is mapped exhaustively so a new SDK case breaks the build rather than mislabelling. Attribution stays in the crypto layer. (2) Verification and recovery state are logged; recovery state is subscribed, not sampled, because it reads `unknown` until the first sync settles. (3) `attemptRestore` now probes for the store directory **before** building the client and skips the orphan sweep when the SDK had to create it. `ClientBuilder` creates the store directory and `restoreSession` succeeds against an empty one — the token and device ID come from the keychain — so a missing store yields a live session with no crypto state beside which every sibling directory looks like an orphan; the sweep would then delete the real store. Defect found: S-06 moved the store parent from a hardcoded `dk.qpqp.mactrix` to the bundle ID without a migration, and a stale pre-S-06 build in DerivedData kept writing to the legacy path. The dev Mac held a store under each parent, and the keychain pointed at the legacy one (`20BBDCDE…`, device `VBCFSGULCL`), so every launch of a current build restored a live session against a freshly created empty store. That, not any verification defect, caused the "re-verify on every launch" symptom: the store was hand-moved under the current parent and verification has survived every restart since. Open finding: `recoveryState` reports `disabled` on an account that has key backup v1 and a recovery key, so this device has no route back to its own history if its store is lost — it survives only because the store survived. Exact `RecoveryState` semantics are unconfirmed; the generated bindings carry no doc comment and the Rust source is not vendored. Filed against S-25. Also filed: S-26 (the `RoomEncryptionBadge` snapshot reference is a 1x render while the dev Mac now produces 2x, so the snapshot gate flips with display backing scale — pre-existing on main and not re-recorded here, since that would turn the branch green and leave the fragility). |
| 2026-08-27 | S-26 snapshot harness: the render scale is now an input to the test, not a property of the machine. `Snapshotting<NSView, NSImage>.image` from swift-snapshot-testing renders through `bitmapImageRepForCachingDisplay(in:)`, which sizes its bitmap from the view's backing scale; a view with no window inherits the main screen's. So every committed reference silently depended on which display was primary when it was recorded, and the failure presented as a layout regression at an identical point size. `ScaledImageSnapshotting` (test-only) builds the `NSBitmapImageRep` by hand — `pixelsWide`/`pixelsHigh` at the point size times a fixed scale, `size` left in points — and renders with `cacheDisplay(in:to:)`, which removes the screen from the path. References are pinned at 2x via `.scaledImage`; `referenceScale` is asserted by a test because changing it invalidates every committed reference at once. `ScaledImageSnapshottingTests` asserts the raster dimensions at 1x, 2x, and 3x, which is the guard against a future change reverting to the screen-derived path — such a change would otherwise pass on the machine that recorded the references and fail everywhere else. The `RoomEncryptionBadge` reference was re-recorded (258x116 for a 129x58 view) and its header comment corrected: it had claimed 2x while the committed file was 1x. Contract R-8 (snapshots as the agent's eyes) now rests on a gate that does not move with the hardware. Font rasterization still varies by OS build, so the CI skip stays. |
| 2026-08-30 | S-23: `davlin-matrix` joined the tailnet, and the S-21 backup transport moved off exe.dev's SSH gateway. Enabled by a Tailscale API integration Patrick added in the exe.dev UI: `https://tailscale.int.exe.xyz` proxies the API with credentials injected, resolves only inside exe.dev, and lets an auth key be minted programmatically — which removed the manual node-authorization step the story had assigned to a human. The node is tagged `tag:matrix-hs` (its own tag, not the existing `tag:davhost-svc`, so a future change to home-service policy cannot widen access to the homeserver) and joined with `--accept-dns=false --accept-routes=false`, deliberately leaving `/etc/resolv.conf` on `1.1.1.1` because Synapse is live on that host and MagicDNS gains nothing here. **Security decision.** Tailscale SSH ignores `authorized_keys` entirely — including the S-21 forced command and its `no-pty`/`no-port-forwarding` options — and executes the account's login shell, which for `synbackup` was `/bin/bash`. Enabling it as the story described would have handed an interactive shell to anyone the ACL admitted, on an account whose sudoers can run `tar -cf -` over `homeserver.signing.key` as root: a homeserver identity compromise, not merely a privilege bump. The whitelist therefore moved into the login shell — `vm-dispatch.sh` is now `synbackup`'s shell and handles both invocation styles, with `SSH_ORIGINAL_COMMAND` taking priority over the `-c` argument because sshd's forced-command path invokes the shell as `-c <the script's own path>` once the script *is* the shell. Verified: `pg-dump` and `tar-config` return byte-identical output through both transports, a dump pulled over the tailnet lists 815 objects under `pg_restore -l`, and interactive login, `bash`, reading the signing key, invoking the sudo `tar` directly, and SSH as `exedev` or `root` are all refused. **ACL decision.** The story's `src: davbuntu` is not expressible — Tailscale rejects hosts and IPs in ssh rules (`"davbuntu": hosts are not allowed`). Tagging davbuntu was rejected because a tagged device leaves `autogroup:self`, which would have silently stripped Patrick's own Tailscale SSH to it; he stated that access is imperative. `src` is therefore the identity `pdavlin@github`, with the narrowing carried by the destination (`tag:matrix-hs`) and the login user (`synbackup`). Changes were additive, validated through the API's own `/acl/validate` before applying, and guarded by preflight assertions that abort unless the pre-existing `autogroup:self` rule, `grants`, `nodeAttrs` and `tag:davhost-svc` are all preserved. Restore point committed at `infra/tailnet/acl-before-s23.json`. **Open, not addressed here:** tailnet `grants` remain `* → *` on all IPs, so network reachability between all 20 devices is unrestricted; only SSH is scoped. Narrowing that touches every device and needs its own story. Also observed: direct SSH to the VM via the exe.dev gateway failed in bursts roughly eight times across 2026-08-29/30, recovering after ~2 minutes each time, while Synapse served throughout — a further argument for the tailnet path carrying the backups. |
| 2026-08-30 | S-21 standing backups are live on davbuntu: `synapse-backup.sh` nightly at 03:17 and `drift-probe.sh` every 30 minutes, both in the `pdavlin` user crontab, pulling from the primary over the S-23 tailnet path. Transport decision: plain `ssh` to the tailnet name `davlin-matrix`, **not** `tailscale ssh` — that wrapper rejects the `-l <user>` argument rsync passes to its `-e` transport, so rsync cannot use it, while plain `ssh` serves both because Tailscale SSH intercepts port 22 on the tailnet name. No key and no `-i`: authentication is the tailnet identity. Alerting decision: `fail()` now pushes to the same ntfy topic the drift probe uses (`davlin-matrix-drift-fce26c1e`), because logging alone is silent under cron — the log lives on the standby box and cron's own mail goes nowhere on a headless host with no MTA, and a nightly backup that fails quietly is worse than none since it looks healthy until a restore is attempted. Verified by breaking it deliberately and polling the topic; a successful run seconds later sent nothing. Restore drill passed: a dump pulled over the tailnet restored into davbuntu's Postgres 16.15 (an exact version match for the primary) and yielded **175 tables** against a >50 threshold. Three faults were found only by executing code that had been committed, reviewed, and never run: (1) `rrsync` refused every media sync with "Not invoked via sshd" because it reads the request from `SSH_ORIGINAL_COMMAND`, which Tailscale SSH never sets — the dispatcher now re-exports what it parsed, and without this the media store would silently never sync while the run reported OK; (2) the rsync source path was `media/` but `rrsync` is already rooted at the media directory, so it resolved to `media/media/` and matched nothing; (3) the davbuntu bootstrap installed postgresql-16 but never created a Postgres role for `pdavlin`, so both the drill and any real failover would have died on `role "pdavlin" does not exist`. A fourth, `drift-probe.sh` never being copied to `/srv/synapse-standby/bin/`, would have failed every 30 minutes forever while appearing installed. Method note recorded because it cost time: the rsync transport was first diagnosed from the Mac and produced a confident wrong answer, since macOS rsync sends options unbundled and Ubuntu's rsync 3.2.7 does not — diagnose transports on the host that will run them. The bootstrap granted no passwordless sudo and none is needed; `pdavlin` owns everything the nightly run touches. |
| 2026-08-31 | S-25 recovery implemented, and `RecoveryState` semantics settled. **Q answered:** `RecoveryState.Disabled` means secret storage is not enabled **on the server** — confirmed two ways, from upstream matrix-rust-sdk at the pinned revision (`check_recovery_state` returns `Disabled` when `secret_storage().is_enabled()` is false, `Incomplete` when it is true but not all secrets are available) and from the dev account, whose `m.secret_storage.default_key` was an empty object (`200 {}`, against `404` for a key never set — the control that made the difference between "cleared" and "absent"). This invalidated the story as written: it specified only a recovery-key entry UI wired to `recover(recoveryKey:)`, which unlocks secret storage that did not exist. The `MATRIX_DEV_RECOVERY_KEY` recorded in the 2026-08-24 S-07 row was therefore dead on arrival — it named storage that had been emptied by the identity reset. **Design:** the full state machine on both surfaces that already carry `VerificationState` — `disabled → enableRecovery`, `incomplete → recover`, `enabled → status`, `unknown → nothing`. Kept visually separate from the verification indicator because a session can be verified and still have no route back to its own history, which is exactly the conflation that cost this account its keys in S-07. The key-display sheet gates dismissal on explicit acknowledgement: `enableRecovery` returns the key once, and only `resetRecoveryKey` issues another, invalidating the first. The key is held in memory for the life of the sheet, never logged, never written to disk, and deliberately not forwarded through `EnableRecoveryProgress.done(recoveryKey:)` into display text, because progress strings end up in logs. **Incident, and the rule it produced:** the first smoke destroyed a live recovery key. `RecoveryState` is derived from account data in the local store, so a freshly logged-in session reports a confident `disabled` — not `unknown` — until its first sync lands. The UI believed it, offered "Set up recovery" to an account that already had one, and enabling replaced it. Two fixes: no actionable recovery UI until `roomListServiceState` is `.running`, and a destructive-role confirmation stating that setup invalidates any existing key. The decision was extracted into `Models.RecoveryPrompt` and unit-tested over all eight state combinations, including the invariant that `offerSetup` is reachable only from a synced `disabled` — the rule that failed is now the rule that is tested. **Verified end to end on the dev account:** a session with no second device and no SAS self-verified from the recovery key alone (`incomplete/unverified → verified → secret storage unlocked → enabled`, inside 600ms), and both states persist across restarts. **Not verified:** that history decrypts after recovery, blocked by S-28 (fresh login yields an empty room list) — the backup decryption key is unlocked and zero UTDs were logged, but nothing loaded, so that is consistent with success rather than evidence of it. |
| 2026-08-31 | Signing: the app now builds with a real development team (`DEVELOPMENT_TEAM = 2BGS8RA3P5`, personal team) instead of ad-hoc. This is S-27 steps 1–2, landed early because it blocked practical work. The keychain matches an item's ACL against the app's designated requirement; for an ad-hoc signature that falls back to the code hash, which changes on every rebuild, so "Always Allow" never persisted and every build cost a dozen manual approvals. Verified by inspecting the signature rather than trusting the setting: `Signature=adhoc, TeamIdentifier=not set` became `TeamIdentifier=2BGS8RA3P5, flags=0x10000(runtime)`, and a subsequent launch after a rebuild and a fresh keychain write produced no prompt at all. Note the hardened runtime is now enabled — the previous build explicitly disabled it for ad-hoc signing — which is a behaviour change, not just metadata. S-27 remains open for the rest: whether a grant survives certificate renewal, and revisiting `kSecUseDataProtectionKeychain` now that a real application identifier exists (the 2026-08-24 Q-5 row deferred that until "a team-signed distribution appears", a condition this creates). **This broke CI and the fix is part of the decision:** GitHub runners hold no certificate for a personal team, so `xcodebuild` failed to sign there. Both CI `xcodebuild` invocations now override back to ad-hoc (`CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY="-" DEVELOPMENT_TEAM=""`), which is exactly what CI signed with before the team existed. Signing identity is therefore environment-specific by design: a real team locally where the keychain ACL matters, ad-hoc on CI where it does not and no certificate exists. |
| 2026-08-31 | S-29 CI failure visibility: the signing break above was diagnosed against a wall — xcpretty, which both CI `xcodebuild` steps piped their output through, consumed every message naming the actual cause, and the whole workflow log said only "** BUILD FAILED **", exit 65. All three `xcodebuild` steps (Build, Test, Build release artifact) now tee their full raw output to a per-step `.log` file and pipe through `xcbeautify --renderer github-actions`, which emits GitHub annotations for compile errors (file, line, message) instead of suppressing them — bare `error:` lines such as the code-signing message above come out as malformed annotations (`::error ::error: …`), so for that class the guaranteed channel is the raw tail and artifacts below, not the annotation layer; passing runs keep the same quiet single-line progress output, so the change costs nothing on green builds. On failure each step dumps the raw tail (last 200 lines) of its log inline in a collapsible `::group::`, preserving the step's exit code, and a final `if: failure()` step uploads the complete logs as workflow artifacts so no failure is ever again visible only in a truncated UI. xcbeautify replaces xcpretty everywhere in CI deliberately: it is the same formatter `scripts/build.sh` already uses, so CI and local builds agree on one formatter rather than diverging again, and only CI passes the `github-actions` renderer because annotations are meaningless off a runner. The local scripts are unchanged — their xcbeautify invocation already degrades to raw output when absent, which is the same no-silent-suppression property this story buys CI. |
| 2026-09-01 | S-24 own-identity reset surfaced in the sidebar. **SDK signal:** `UserIdentity.hasVerificationViolation()` (FFI: `docs/ffi/matrix_sdk_ffi.swift:16898`, protocol method on `UserIdentityProtocol`) reports `true` when the identity was previously verified but no longer is — the exact signal for an own-identity reset (e.g. from Element Web while Mactrix had the old identity pinned). Combined with `wasPreviouslyVerified()` (FFI: `docs/ffi/matrix_sdk_ffi.swift:16942`) to distinguish a genuine reset from a session that was never verified. The `VerificationState` enum (`unknown`/`verified`/`unverified`, FFI: `docs/ffi/matrix_sdk_ffi.swift:40075`) does not carry a "reset" variant, so the identity check is an additional query against `encryption.userIdentity(userId:fallbackToServer:)` (FFI: `docs/ffi/matrix_sdk_ffi.swift:4115`), triggered when `verificationState` changes via the existing `VerificationStateListener`. **Design decision:** `IdentityResetPrompt.decide()` in `MactrixLibrary/Sources/Models/RecoveryPrompt.swift` mirrors the `RecoveryPrompt` pattern — pure decision logic, unit-tested over all eight state combinations, with the same sync gate (`roomListServiceState == .running`) that protects recovery prompts from acting on stale identity data from an empty store. The banner (`IdentityResetBanner` in `MactrixLibrary/Sources/UI/IdentityResetBanner.swift`) appears in the sidebar above the ordinary unverified prompt and the recovery status, with two recovery actions: re-verify (wired to the existing `requestDeviceVerification()` flow) and sign out (wired to `AppState.reset()`). **Not a listener:** the SDK exposes no dedicated identity-change listener; the check piggybacks on `VerificationStateListener.onUpdate()`, which fires on sync state changes. If the identity is reset while the app is running and no verification state change occurs, the banner will appear on the next verification state update (e.g. next sync cycle). This is acceptable because identity resets are rare and the user will notice SAS failures before the banner anyway. |
